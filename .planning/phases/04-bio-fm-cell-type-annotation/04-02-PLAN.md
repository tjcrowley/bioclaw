---
phase: 04-bio-fm-cell-type-annotation
plan: 02
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - annotation/baseline.py
  - tests/test_annotation_baseline.py
autonomous: true
requirements: ["ANNOT-02"]

must_haves:
  truths:
    - "baseline_annotate() runs a decoupler ORA-family marker-gene enrichment per group in an already-clustered/labeled AnnData and returns one AnnotationCall per group, never per cell"
    - "Every returned AnnotationCall has a non-null label, a numeric confidence/score, and a reference_dataset string naming the marker resource used"
    - "Unit tests are network-free and deterministic -- no live call to decoupler's resource-fetching API during tests"
  artifacts:
    - path: "annotation/baseline.py"
      provides: "baseline_annotate(adata, groupby, markers=None, resource_name='PanglaoDB') -> list[AnnotationCall]"
      exports: ["baseline_annotate"]
  key_links:
    - from: "annotation/baseline.py"
      to: "decoupler (dc.op.resource / dc.mt.ora or the installed package's current v2 equivalent)"
      via: "direct function call, introspected against the installed decoupler==2.2.0 package before implementation"
      pattern: "dc\\.(op|mt)\\."
    - from: "annotation/baseline.py"
      to: "annotation/summary.py:AnnotationCall"
      via: "return type"
      pattern: "AnnotationCall\\("
---

<objective>
Implement `annotation/baseline.py::baseline_annotate()` -- the `decoupler`-backed marker-gene ORA statistical baseline that ANNOT-02 requires alongside every FM-backed annotation call.

Purpose: Per 04-RESEARCH.md Pattern 2 and Pitfall 3, `decoupler`'s public API moved from a v1.x (`dc.get_resource`/`dc.run_ora`) to a v2.x (`dc.op.*`/`dc.mt.*`) module layout, and the exact current function signatures were not directly verifiable during research -- this plan's first step is confirming the installed `decoupler==2.2.0` package's actual API via direct introspection, not trusting the research sketch verbatim. The baseline call is unconditional and independent of scGPT/the isolated environment, so it can be fully implemented and unit-tested now, in parallel with Plan 04-03's isolated-environment work.
Output: `annotation/baseline.py` with a working `baseline_annotate()`, passing `tests/test_annotation_baseline.py`.
</objective>

<execution_context>
@/Users/darren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darren/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-bio-fm-cell-type-annotation/04-RESEARCH.md
@.planning/phases/04-bio-fm-cell-type-annotation/04-VALIDATION.md
@annotation/summary.py
@tests/conftest.py
</context>

<interfaces>
<!-- From annotation/summary.py (Plan 04-01, already exists). -->
```python
@dataclass
class AnnotationCall:
    cluster: str
    label: str
    confidence: float
    reference_dataset: str
    ontology_term_id: str | None
```

<!-- tests/conftest.py already provides structured_adata: an in-memory
     AnnData (200 cells x 80 genes) with two well-separated pseudo-populations
     in adata.obs["true_population"] ("A"/"B"). Population A: genes GENE00-14
     elevated (Poisson lam=15) vs. background (lam=2). Population B: genes
     GENE15-29 elevated. Use this fixture directly with groupby="true_population"
     -- no need to run real clustering first, since baseline_annotate() only
     needs a groupby column with group labels, not specifically "leiden". -->
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: baseline_annotate() -- decoupler ORA marker-gene baseline</name>
  <files>annotation/baseline.py, tests/test_annotation_baseline.py</files>
  <behavior>
    - Before writing any implementation, run `uv run python -c "import decoupler as dc; help(dc.op); help(dc.mt)"` (and `dir(dc.op)`, `dir(dc.mt)`) against the actually-installed `decoupler==2.2.0` to confirm the exact current function names/kwargs for resource retrieval and an ORA-family (or successor) enrichment method. Do NOT trust 04-RESEARCH.md Pattern 2's `dc.op.resource(...)`/`dc.mt.ora(...)` sketch verbatim -- it is explicitly flagged LOW-MEDIUM confidence on exact signatures. Record whatever the real introspected signature is in the module docstring, mirroring how `agent/tools.py`'s docstring records Phase 3's own introspection findings.
    - `baseline_annotate(adata, groupby="leiden", markers=None, resource_name="PanglaoDB")` accepts an already-labeled/clustered `AnnData` (a `groupby` column must already exist in `adata.obs` -- this function never runs clustering itself) and an optional `markers: pd.DataFrame | None` parameter. When `markers` is `None`, the real (non-test) code path fetches a live resource via the introspected decoupler resource-retrieval call (network access at runtime -- acceptable in production, never in unit tests). When `markers` is provided, it is used directly, with no network call -- this is the seam unit tests use to stay network-free.
    - Unit tests construct a small, hand-crafted, in-memory marker resource DataFrame (matching `decoupler`'s expected column shape, e.g. `source`=cell type label, `target`=gene symbol) using `structured_adata`'s own known marker genes: `GENE00`-`GENE14` for a "Population A"-like label, `GENE15`-`GENE29` for a "Population B"-like label. Calling `baseline_annotate(structured_adata, groupby="true_population", markers=<synthetic resource>)` returns exactly 2 `AnnotationCall` objects (one per group "A"/"B"), each with a non-null `label`, a numeric `confidence` (the ORA/enrichment score, or `-log10(p)`, or whatever the introspected method actually returns -- must be `float`, not `None`), `reference_dataset` containing `resource_name` (e.g. `"decoupler ORA vs PanglaoDB (human, canonical markers)"` or similar descriptive string), and `ontology_term_id is None` (PanglaoDB/CellMarker resources carry no Cell Ontology terms -- this is a permanent, documented property of the baseline side, not a bug; see module docstring).
    - Test asserts group "A" cells' top-scoring call has a label distinguishable from group "B"'s (the synthetic marker resource is constructed so population A's marker genes clearly enrich against the "A"-labeled resource entry and not the "B"-labeled entry, proving the statistical logic actually discriminates, not just returns a fixed label).
    - No test calls the real network-backed resource-fetching path -- confirm this by asserting the test suite runs to completion with no network access available (e.g. no explicit network mocking needed if `markers` is always passed in tests, since the code path that would call the network is simply never exercised).
  </behavior>
  <action>
    Implement `annotation/baseline.py`:
    - Module docstring records the introspected decoupler v2 API findings (function names/kwargs actually used).
    - `baseline_annotate(adata, groupby="leiden", markers=None, resource_name="PanglaoDB")`:
      1. If `markers is None`, fetch the named resource via the introspected decoupler API call (network path, production only).
      2. Run the introspected ORA-family (or successor) enrichment method against `adata`/`markers`, grouped by `adata.obs[groupby]`.
      3. For each unique value in `adata.obs[groupby]`, extract the top-scoring label and its score from the enrichment result.
      4. Return `[AnnotationCall(cluster=str(group), label=top_label, confidence=float(top_score), reference_dataset=f"decoupler ORA vs {resource_name} (human, canonical markers)", ontology_term_id=None) for group in groups]`.

    Write `tests/test_annotation_baseline.py` first (RED, using `structured_adata` and the synthetic marker DataFrame described above), then implement `baseline.py` until green.
  </action>
  <verify>
    <automated>uv run pytest tests/test_annotation_baseline.py -x -q</automated>
  </verify>
  <done>`uv run pytest tests/test_annotation_baseline.py -x -q` passes -- covers the 2-group round trip on `structured_adata`, non-null/float `confidence`, `reference_dataset` naming the resource, `ontology_term_id is None` for every baseline call, and label discrimination between the two populations.</done>
</task>

</tasks>

<verification>
`uv run pytest tests/ -q -m "not live_llm and not bio_fm_smoke"` fully green (Phase 1-3 tests + new `test_annotation_baseline.py`). No network access required to pass.
</verification>

<success_criteria>
- `baseline_annotate()` is a real, tested, network-free-by-default (in tests) ORA-based marker-gene baseline over `decoupler`'s actual installed v2 API, not the research sketch's unverified guess.
- Every `AnnotationCall` it returns satisfies ANNOT-02/ANNOT-03's non-bare-label requirement (label + confidence + reference_dataset, `ontology_term_id` intentionally `None` on this side).
- Fully independent of the isolated scGPT environment (Plan 04-03) -- can run in parallel.
</success_criteria>

<output>
After completion, create `.planning/phases/04-bio-fm-cell-type-annotation/04-02-SUMMARY.md`
</output>
