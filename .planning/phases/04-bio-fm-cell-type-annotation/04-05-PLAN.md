---
phase: 04-bio-fm-cell-type-annotation
plan: 05
type: execute
wave: 3
depends_on: ["04-03", "04-04"]
files_modified:
  - pyproject.toml
  - annotation/reference.py
  - bio_fm_worker/checkpoints/
  - tests/test_bio_fm_integration.py
autonomous: false
requirements: ["ANNOT-01", "ANNOT-03"]
user_setup:
  - service: scGPT pretrained checkpoint
    why: "ANNOT-01's zero-shot reference-mapping call needs a real pretrained scGPT 'whole-human' checkpoint; the model's maintainers distribute it via a hosted file share (commonly Google Drive) that a scripted download can hit an interactive/quota wall on"
    dashboard_config:
      - task: "If the automated download attempt in Task 2 fails, manually download the scGPT 'whole-human' checkpoint per the instructions in github.com/bowang-lab/scGPT's README and place it at bio_fm_worker/checkpoints/scGPT_human/"
        location: "github.com/bowang-lab/scGPT README, 'Pretrained scGPT Model Zoo' section"

must_haves:
  truths:
    - "A small, static, cellxgene-census-sourced reference dataset exists on disk with cell_type and cell_type_ontology_term_id per cell, ready for run_scgpt_embed.py to embed"
    - "At least one real, measured, end-to-end scGPT annotation call has been demonstrated against the isolated environment, with wall-clock latency recorded (closes STATE.md's compute-sizing blocker with real evidence, not estimation)"
    - "The bio_fm_smoke-marked integration test is excluded from the default fast-tier test run"
  artifacts:
    - path: "annotation/reference.py"
      provides: "build_reference_index(tissue, n_cells, out_path) -> Path, querying cellxgene-census for a labeled reference subsample"
      exports: ["build_reference_index"]
    - path: "tests/test_bio_fm_integration.py"
      provides: "bio_fm_smoke-marked end-to-end test: ingest -> analyze -> annotate_cell_type_tool against the real isolated environment and checkpoint"
  key_links:
    - from: "annotation/reference.py"
      to: "cellxgene-census (public API, no auth)"
      via: "cellxgene_census.open_soma() / get_anndata()-style query for a small labeled subsample"
      pattern: "cellxgene_census\\."
    - from: "tests/test_bio_fm_integration.py"
      to: "agent/tools.py:annotate_cell_type_tool"
      via: "real (unmocked) end-to-end call, marked @pytest.mark.bio_fm_smoke"
      pattern: "bio_fm_smoke"
---

<objective>
Close the phase's last gap: build a real, static `cellxgene-census`-sourced reference embedding index, acquire the scGPT pretrained checkpoint into the isolated `bio_fm_worker/` environment, and demonstrate/measure one real, end-to-end `annotate_cell_type` tool call -- the Phase Gate manual verification 04-VALIDATION.md's Phase Gate row requires before `/gsd:verify-work`.

Purpose: Per 04-RESEARCH.md's Don't Hand-Roll section, `cellxgene-census`'s CELLxGENE-schema-enforced `cell_type_ontology_term_id` field is the entire ANNOT-03 ontology-metadata answer -- no separate ontology-mapping pipeline needed, only a small labeled reference subsample. Per Pitfall 2, real scGPT CPU-inference latency was only estimated from one unverified third-party benchmark during research -- this plan is where that estimate becomes a real, measured number against this project's own hardware and fixtures. Per STATE.md's own flagged blocker ("scGPT/Geneformer VRAM requirements are LOW confidence... verify against current model cards before implementation"), this plan is the actual verification: Tasks 1-2 automate everything Claude can do via CLI/API (querying the public `cellxgene-census` API needs no authentication; attempting the checkpoint download is scripted first); Task 3 is a `checkpoint:human-verify` only for whatever automation cannot complete (a Google-Drive-hosted large-file download commonly hits an interactive confirmation/quota wall that no CLI credential can bypass).
Output: `annotation/reference.py` with a working `build_reference_index()`, a built reference index file, a `bio_fm_smoke`-marked integration test, and a demonstrated real annotation call with recorded latency.
</objective>

<execution_context>
@/Users/darren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darren/.claude/get-shit-done/templates/summary.md
@/Users/darren/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-bio-fm-cell-type-annotation/04-RESEARCH.md
@.planning/phases/04-bio-fm-cell-type-annotation/04-VALIDATION.md
@annotation/pipeline.py
@annotation/fm_client.py
@bio_fm_worker/run_scgpt_embed.py
@agent/tools.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: build_reference_index() -- cellxgene-census reference subsample</name>
  <files>pyproject.toml, annotation/reference.py</files>
  <action>
    Run `uv add cellxgene-census` in the main venv (per 04-RESEARCH.md Standard Stack, this library has no `torch` dependency -- confirm this holds by checking the resolved dependency tree does not pull in `torch`; if it unexpectedly does, stop and flag it, since that would contradict the research finding and change the isolation calculus).

    Implement `annotation/reference.py::build_reference_index(tissue="blood", n_cells=3000, out_path="bio_fm_worker/reference/reference.h5ad") -> Path`:
    - Opens the public `cellxgene-census` API (no authentication required) and queries a random subsample of `n_cells` cells from the given `tissue`, requesting at minimum `cell_type`, `cell_type_ontology_term_id`, and normalized/raw expression matching whatever gene identifier scGPT's embedding call expects (confirm the exact `cellxgene_census` function names via `python -c "import cellxgene_census; help(cellxgene_census)"` before implementing -- do not assume specific function names from memory).
    - Writes the resulting `AnnData` to `out_path` via `write_h5ad`, ensuring `.obs["cell_type"]` and `.obs["cell_type_ontology_term_id"]` are both present and non-null for every cell (drop/re-sample any cells missing either field -- the whole point of using `cellxgene-census` is this metadata is guaranteed present, so failures here indicate a query mistake, not a data gap).
    - Returns the `Path` to the written file.
    - Document in the module docstring: this is a one-time (or occasional, re-run manually) build step, not queried live on every annotation call -- matches 04-RESEARCH.md Open Question 4's recommendation to start with a small, static, single-tissue reference.

    Actually run `build_reference_index()` once now (e.g. via `uv run python -c "from annotation.reference import build_reference_index; print(build_reference_index())"`) to produce the real reference file at `bio_fm_worker/reference/reference.h5ad` used by Task 3's smoke test. Add `bio_fm_worker/reference/` to `.gitignore` if the resulting file is large (check its size; if under a few MB it's fine to commit, otherwise gitignore it and document the rebuild command in `bio_fm_worker/README.md`).
  </action>
  <verify>
    <automated>uv run python -c "from annotation.reference import build_reference_index; print('ok')"</automated>
  </verify>
  <done>`cellxgene-census` is installed with no `torch` in its resolved dependency tree; `build_reference_index()` is implemented and has been run at least once, producing a real `.h5ad` file with non-null `cell_type`/`cell_type_ontology_term_id` for every cell.</done>
</task>

<task type="auto">
  <name>Task 2: Acquire the scGPT checkpoint (automated attempt) + write the bio_fm_smoke integration test</name>
  <files>bio_fm_worker/checkpoints/, tests/test_bio_fm_integration.py</files>
  <action>
    Attempt to download the scGPT pretrained "whole-human" checkpoint into `bio_fm_worker/checkpoints/scGPT_human/` following the exact instructions in `github.com/bowang-lab/scGPT`'s README ("Pretrained scGPT Model Zoo" section) -- fetch that README's current content first (`WebFetch` or `curl`) to get the actual current download link/method, since 04-RESEARCH.md did not pin this down. If the link is a direct downloadable URL, use `curl`/`wget`. If it requires `gdown` (common for Google-Drive-hosted files), try `pip install gdown` into `bio_fm_worker/.venv` and run it. If the download fails, hits an interactive confirmation page, or a quota/rate-limit wall, stop attempting workarounds (do not try to script around an anti-automation control) and record the exact failure in this plan's SUMMARY -- Task 3's checkpoint will surface this to the user.

    Write `tests/test_bio_fm_integration.py`:
    ```python
    import time
    import pytest

    pytestmark = pytest.mark.bio_fm_smoke

    def test_annotate_cell_type_tool_real_scgpt_inference(tmp_path, analyzable_mtx_dir_like_fixture, monkeypatch):
        # ingest -> analyze -> annotate_cell_type_tool, UNMOCKED, against the
        # real isolated environment and the checkpoint acquired above.
        # Record wall-clock latency around the annotate_cell_type_tool.handler(...)
        # call and print/assert it is within a sane bound (document whatever
        # bound is chosen and why, referencing Pitfall 2's ~5164s/140k-cells
        # estimate scaled to this fixture's cell count).
        ...
    ```
    Build this test's own fixture (reuse or adapt `analyzable_mtx_dir` from `tests/test_agent_tools.py`, or import it directly) so the test is self-contained and can run via `uv run pytest tests/test_bio_fm_integration.py -m bio_fm_smoke -x` without further setup once the checkpoint exists.
  </action>
  <verify>
    <automated>uv run pytest tests/ -q -m "not live_llm and not bio_fm_smoke" && uv run python -m py_compile tests/test_bio_fm_integration.py</automated>
  </verify>
  <done>`tests/test_bio_fm_integration.py` exists, is marked `bio_fm_smoke`, and is excluded from the default fast-tier run (verified by the first half of the verify command staying green); the checkpoint download was attempted and its outcome (success or the specific failure) is documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify real scGPT checkpoint + end-to-end annotation smoke test</name>
  <files>bio_fm_worker/checkpoints/scGPT_human/</files>
  <action>
    Everything automatable has already been done by Tasks 1-2: the reference index is built, the checkpoint download was attempted, and the bio_fm_smoke test is written. This checkpoint exists only for whatever automation could not complete (typically an interactive/quota-walled checkpoint download) and for a human sanity check on the real inference result.
  </action>
  <what-built>
    A cellxgene-census-sourced reference index (`bio_fm_worker/reference/reference.h5ad`), an attempted automated download of the scGPT pretrained checkpoint, and a `bio_fm_smoke`-marked end-to-end integration test (`tests/test_bio_fm_integration.py`) ready to run once the checkpoint is in place.
  </what-built>
  <how-to-verify>
    1. If Task 2's automated checkpoint download failed, manually download the scGPT "whole-human" checkpoint per `github.com/bowang-lab/scGPT`'s README instructions and place it at `bio_fm_worker/checkpoints/scGPT_human/` (matching whatever directory structure `run_scgpt_embed.py`'s `--model-dir` argument expects).
    2. Run `uv run pytest tests/test_bio_fm_integration.py -m bio_fm_smoke -x -v` and report: did it pass, and what was the wall-clock latency for the real scGPT embedding + reference-mapping call?
    3. Confirm the annotation result looks plausible (a real cell-type label, not an obvious error string) for at least one cluster.
    4. If the smoke test fails with an import/install error (rather than a missing-checkpoint error), the isolated venv's `pip install scgpt` from Plan 04-03 Task 1 likely never succeeded -- check `bio_fm_worker/README.md`'s documented install outcome first before debugging the checkpoint/inference path itself.
  </how-to-verify>
  <verify>
    <automated>MISSING -- inherently manual: requires a downloaded multi-GB pretrained checkpoint and real (non-mocked) scGPT inference, excluded from CI via the bio_fm_smoke marker (see Task 2)</automated>
  </verify>
  <done>Human reports "passed, latency: Xs" with a plausible real annotation result for at least one cluster, or a documented reason it could not be completed (checkpoint unavailable, etc.).</done>
  <resume-signal>Report "passed, latency: Xs" or describe what failed (checkpoint download issue, scGPT API mismatch, latency far exceeding expectations, etc.)</resume-signal>
</task>

</tasks>

<verification>
`uv run pytest tests/ -q -m "not live_llm and not bio_fm_smoke"` fully green (unchanged from Wave 2 -- this plan adds no new fast-tier tests). The `bio_fm_smoke` test has been run at least once manually with a recorded outcome, per the checkpoint above.
</verification>

<success_criteria>
- `annotation/reference.py::build_reference_index()` produces a real, checked reference dataset with guaranteed `cell_type`/`cell_type_ontology_term_id` metadata -- closing ANNOT-03's ontology-metadata requirement with real data, not just a dataclass field.
- The scGPT checkpoint is either successfully acquired (automated or manual) or its acquisition failure is clearly documented as a known, human-actionable gap.
- STATE.md's Phase 4 compute-sizing blocker is closed with a real, measured latency number (or a clearly documented reason it could not be measured yet).
</success_criteria>

<output>
After completion, create `.planning/phases/04-bio-fm-cell-type-annotation/04-05-SUMMARY.md`
</output>
