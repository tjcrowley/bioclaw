---
phase: 03-agent-orchestration-wiring
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - agent/__init__.py
  - .gitignore
autonomous: true
requirements: []

must_haves:
  truths:
    - "claude_agent_sdk is installed and importable, so @tool/create_sdk_mcp_server/ClaudeSDKClient can be used by every Wave 1/2 plan"
    - "live_llm is a registered pytest marker, so the fast test tier can reliably exclude live-API tests via -m \"not live_llm\""
    - "agent/ exists as a real Python package Wave 1/2 plans add modules into"
  artifacts:
    - path: "pyproject.toml"
      provides: "claude-agent-sdk dependency + live_llm custom pytest marker registration"
    - path: "agent/__init__.py"
      provides: "agent package skeleton"
  key_links:
    - from: "pyproject.toml"
      to: "tests/ (fast-tier filtering)"
      via: "[tool.pytest.ini_options] markers = [\"live_llm: ...\"]"
      pattern: "live_llm"
---

<objective>
Stand up Phase 3's infrastructure: install `claude-agent-sdk`, register the `live_llm` pytest marker so live-API tests can be excluded from the fast/default suite, create the `agent/` package skeleton, and gitignore the runtime artifacts (`agent/logs/`, `agent/memory.sqlite`) later Wave 1/2 plans will write.

Purpose: Per 03-RESEARCH.md, no `@tool`/`create_sdk_mcp_server`/`ClaudeSDKClient` import can succeed until `claude-agent-sdk` is a real dependency, and per the two-tier testing strategy (Pitfall 4), the fast default suite must be able to exclude the live-LLM smoke test via a registered marker before any Wave 1 test file is written.
Output: `claude-agent-sdk` installed, `live_llm` marker registered, `agent/` package importable, gitignore updated.
</objective>

<execution_context>
@/Users/darren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darren/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agent-orchestration-wiring/03-RESEARCH.md
@.planning/phases/03-agent-orchestration-wiring/03-VALIDATION.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install claude-agent-sdk + register live_llm pytest marker</name>
  <files>pyproject.toml</files>
  <action>
    Run `uv add claude-agent-sdk` from repo root -- adds the dependency to `pyproject.toml` and installs it into `.venv`. This requires Python `>=3.10`; this repo is already on `>=3.12` per `pyproject.toml`'s `requires-python`, so no floor bump needed.

    Add a `markers` list to the existing `[tool.pytest.ini_options]` table (alongside the existing `testpaths`/`pythonpath` keys, do not remove them):
    ```toml
    [tool.pytest.ini_options]
    testpaths = ["tests"]
    pythonpath = ["."]
    markers = [
        "live_llm: requires a live Anthropic API key (ANTHROPIC_API_KEY), excluded from fast/CI runs",
    ]
    ```
    Verify with `uv run python -c "import claude_agent_sdk; print('ok')"` and `uv run pytest --markers | grep live_llm`.
  </action>
  <verify>
    <automated>uv run python -c "import claude_agent_sdk" && uv run pytest --markers | grep -q live_llm</automated>
  </verify>
  <done>`claude-agent-sdk` is listed in `pyproject.toml` dependencies and importable; `uv run pytest --markers` lists `live_llm`.</done>
</task>

<task type="auto">
  <name>Task 2: Create agent/ package skeleton + gitignore runtime artifacts</name>
  <files>agent/__init__.py, .gitignore</files>
  <action>
    Create `agent/__init__.py` (empty, marks the package -- mirrors `analysis/__init__.py`'s precedent from Phase 2's 02-01 plan). Do not create `agent/logs/` as a tracked directory -- it's created at runtime by `agent/logging.py` (Wave 1) via `Path.mkdir(parents=True, exist_ok=True)`.

    Append to the existing `.gitignore` (which already has `*.h5ad`, `data/`, etc.):
    ```
    agent/logs/
    agent/memory.sqlite
    ```
    This mirrors how `data/` (Phase 1's `DatasetStore` output) is already gitignored -- runtime state, not source.
  </action>
  <verify>
    <automated>uv run python -c "import agent; print('ok')" && grep -q "agent/logs/" .gitignore && grep -q "agent/memory.sqlite" .gitignore</automated>
  </verify>
  <done>`agent` package is importable; `.gitignore` excludes `agent/logs/` and `agent/memory.sqlite`.</done>
</task>

</tasks>

<verification>
`uv run pytest tests/ -q -m "not live_llm"` still fully green (no new tests yet, only Phase 1/2 tests exist at this point). `uv run python -c "import claude_agent_sdk, agent"` succeeds.
</verification>

<success_criteria>
- `claude-agent-sdk` is a real, importable project dependency.
- The fast test tier can reliably exclude live-API tests via the registered `live_llm` marker before any such test exists.
- `agent/` is a real Python package Wave 1 plans (`agent/tools.py`, `agent/logging.py`, `agent/memory.py`) add modules into, with its runtime output directories gitignored.
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent-orchestration-wiring/03-01-SUMMARY.md`
</output>
