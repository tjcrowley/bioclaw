---
phase: 02-analysis-tool-layer
plan: 04
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - analysis/diffexp.py
  - tests/test_diffexp.py
autonomous: true
requirements: ["ANLYS-03"]

must_haves:
  truths:
    - "Wilcoxon rank-sum differential expression between two groups (clusters or arbitrary condition labels) returns a ranked gene result"
    - "The known marker genes baked into structured_adata's population design are recovered as top DE hits when comparing population A vs B"
    - "differential_expression() is generic over any categorical .obs column, not hard-coded to a Leiden cluster key"
  artifacts:
    - path: "analysis/diffexp.py"
      provides: "differential_expression(adata, groupby, group1, group2=None, **config) -> (AnnData, DESummary)"
  key_links:
    - from: "analysis/diffexp.py"
      to: "analysis/summary.py:DESummary, DEGeneResult"
      via: "constructs and returns DESummary with capped top_genes"
      pattern: "DESummary\\("
---

<objective>
Build `analysis/diffexp.py` (ANLYS-03): Wilcoxon rank-sum DE generalized over any categorical `.obs` column (cluster key or condition key), using `sc.get.rank_genes_groups_df` for bounded extraction, returning a capped `DESummary`.

Purpose: Per `02-RESEARCH.md` Open Question 3, `sc.tl.rank_genes_groups`'s `groupby`/`groups`/`reference` params are already fully generic -- nothing scanpy-specific ties DE to the Leiden cluster key, and the phase requirement text ("between two clusters or conditions") implies both must work through the same function. Per Don't Hand-Roll guidance, extraction must use `sc.get.rank_genes_groups_df`, never hand-parsed `adata.uns['rank_genes_groups']` structured arrays.
Output: `analysis/diffexp.py`, passing tests.
</objective>

<execution_context>
@/Users/darren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darren/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-analysis-tool-layer/02-RESEARCH.md
@.planning/phases/02-analysis-tool-layer/02-VALIDATION.md
</context>

<interfaces>
<!-- From Plan 02-01 (analysis/summary.py) -- use these dataclasses directly, no exploration needed. -->

```python
# analysis/summary.py (already exists from Plan 02-01)
@dataclass
class DEGeneResult:
    gene: str
    score: float
    pval: float
    pval_adj: float
    logfoldchange: float
    pct_group: float | None
    pct_rest: float | None

@dataclass
class DESummary:
    dataset_id: str | None
    groupby: str
    group1: str
    group2: str | None
    method: str
    n_genes_tested: int
    n_significant: int
    top_genes: list[DEGeneResult]
```

<!-- From Plan 02-01 (tests/conftest.py). This plan does NOT depend on Plan 02-02 or 02-03
     -- it runs independently in the same wave, using structured_adata's built-in
     `.obs["true_population"]` ground-truth column directly as the groupby target
     (no need to run cluster.py first). -->

```python
# tests/conftest.py (already exists)
def structured_adata() -> AnnData:
    """~200 cells x ~80 genes, raw integer counts. .obs['true_population'] in
    {"A", "B"}. Population A: genes 0-14 elevated. Population B: genes 15-29
    elevated. Background lam=2 Poisson noise everywhere else."""
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: differential_expression() -- generic Wilcoxon DE via rank_genes_groups</name>
  <files>analysis/diffexp.py, tests/test_diffexp.py</files>
  <behavior>
    - `differential_expression(structured_adata, groupby="true_population", group1="A", group2="B", n_genes=25)` returns a DataFrame/result where at least half of the returned top genes are drawn from `{GENE00..GENE14}` (population A's known elevated marker block) -- proves the fixture's known structure is actually recovered by real Wilcoxon statistics, not just "returns something."
    - `differential_expression(structured_adata, groupby="true_population", group1="A", group2=None)` (comparing vs. "rest", i.e. group B since only two groups exist) behaves equivalently to the explicit `group2="B"` call for this two-group fixture.
    - Result is generic over any categorical `.obs` column -- re-running with a synthetically added `.obs["condition"]` column (test constructs this inline, e.g. alternating labels unrelated to `true_population`) also succeeds and returns a well-formed result, proving `groupby` is not hard-coded to `"leiden"`.
    - Uses `method="wilcoxon"`, `tie_correct=True`, `pts=True`, `corr_method="benjamini-hochberg"` -- assert these appear in `adata.uns["rank_genes_groups"]["params"]` after the call.
  </behavior>
  <action>
    Implement `analysis/diffexp.py::differential_expression(adata, groupby, group1, group2=None, *, n_genes=25, tie_correct=True)` per `02-RESEARCH.md` Pattern 3: `adata = adata.copy()`, `reference = group2 if group2 is not None else "rest"`, `sc.tl.rank_genes_groups(adata, groupby=groupby, groups=[group1], reference=reference, method="wilcoxon", tie_correct=tie_correct, pts=True, corr_method="benjamini-hochberg")`, extract via `sc.get.rank_genes_groups_df(adata, group=group1)` -- never hand-parse `adata.uns["rank_genes_groups"]`'s structured arrays. Return the mutated `adata` only (summary construction is Task 2). Write `tests/test_diffexp.py` first (RED) per `<behavior>`, then implement until green.
  </action>
  <verify>
    <automated>uv run pytest tests/test_diffexp.py -x -q</automated>
  </verify>
  <done>`uv run pytest tests/test_diffexp.py -x -q` passes -- covers marker-gene recovery, the `group2=None` "rest" default, and genericity over an arbitrary `.obs` column.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DESummary/DEGeneResult construction (ANLYS-04 for this module)</name>
  <files>analysis/diffexp.py, tests/test_diffexp.py</files>
  <behavior>
    - `differential_expression()` now returns `(adata, DESummary)`. `summary.top_genes` has `len() <= n_genes` (default 25) regardless of how many genes were tested (`structured_adata` has 80 -- all tested, only top 25 returned); each `DEGeneResult` has non-null `gene`/`score`/`pval`/`pval_adj`/`logfoldchange` and populated `pct_group`/`pct_rest` (from `pts=True`). `summary.n_genes_tested == 80`, `summary.n_significant` equals the count of tested genes with `pval_adj < 0.05`. `summary.groupby`/`group1`/`group2`/`method` match the call inputs. `summary.dataset_id is None`.
    - `dataclasses.asdict(summary)` never contains the full 80-gene ranked table -- only the capped `top_genes` list.
  </behavior>
  <action>
    Extend `differential_expression()` to build and return a `DESummary` (imported from `analysis.summary`): sort the `sc.get.rank_genes_groups_df` result by `pvals_adj` ascending, `.head(n_genes)`, map each row to a `DEGeneResult` (rename `pvals`/`pvals_adj`/`logfoldchanges`/`pct_nz_group`/`pct_nz_reference` to the dataclass's field names), compute `n_significant` from the *full* (unsliced) result DataFrame before truncation, set `dataset_id=None`. Update the function signature return type to `tuple[AnnData, DESummary]`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_diffexp.py -x -q</automated>
  </verify>
  <done>`uv run pytest tests/test_diffexp.py -x -q` passes, including the bounded-summary and marker-gene-recovery assertions.</done>
</task>

</tasks>

<verification>
`uv run pytest tests/test_diffexp.py -q` all green. `uv run pytest tests/ -q` (full suite so far) still green.
</verification>

<success_criteria>
- `differential_expression()` returns Wilcoxon-ranked genes that correctly recover `structured_adata`'s known marker-gene structure.
- Fully generic over `groupby`/`group1`/`group2` -- works for cluster keys and arbitrary condition labels alike, satisfying "between two clusters or conditions."
- Returns a `DESummary` whose `top_genes` is always capped, never the full per-gene ranked table.
</success_criteria>

<output>
After completion, create `.planning/phases/02-analysis-tool-layer/02-04-SUMMARY.md`
</output>
