---
phase: 02-analysis-tool-layer
plan: 03
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - analysis/cluster.py
  - tests/test_cluster.py
autonomous: true
requirements: ["ANLYS-02"]

must_haves:
  truths:
    - "Cells cluster via Leiden (flavor=\"igraph\") into >=2 groups on a dataset with real population structure"
    - "A 2D UMAP embedding is produced for any clustered dataset"
    - "Leiden cluster assignments are deterministic given a fixed random_state; UMAP coordinates are not asserted byte-exact (known upstream nondeterminism)"
  artifacts:
    - path: "analysis/cluster.py"
      provides: "cluster(adata, **config) -> (AnnData, ClusterSummary)"
  key_links:
    - from: "analysis/cluster.py"
      to: "analysis/summary.py:ClusterSummary"
      via: "constructs and returns ClusterSummary"
      pattern: "ClusterSummary\\("
---

<objective>
Build `analysis/cluster.py` (ANLYS-02): neighbor graph -> Leiden (`flavor="igraph"`) -> UMAP, guarded against small inputs, with Leiden cluster labels deterministic given a fixed `random_state`, returning a bounded `ClusterSummary`.

Purpose: Per `02-RESEARCH.md` Pitfall 2, `flavor="igraph"` raises `ValueError` if `directed=True` is passed -- this must never happen. Per Pitfall 5, UMAP's own coordinate reproducibility has a known upstream nondeterminism issue (`umap-learn` GitHub #1080/#1108) even with a fixed seed -- this plan validates determinism on Leiden labels (fully deterministic, no threading ambiguity) rather than UMAP coordinates.
Output: `analysis/cluster.py`, passing tests.
</objective>

<execution_context>
@/Users/darren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darren/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-analysis-tool-layer/02-RESEARCH.md
@.planning/phases/02-analysis-tool-layer/02-VALIDATION.md
</context>

<interfaces>
<!-- From Plan 02-01 (analysis/summary.py) -- use this dataclass directly, no exploration needed. -->

```python
# analysis/summary.py (already exists from Plan 02-01)
@dataclass
class ClusterSummary:
    dataset_id: str | None
    n_clusters: int
    cluster_sizes: dict[str, int]
    resolution: float
    umap_computed: bool
    config: dict
```

<!-- From Plan 02-01 (tests/conftest.py). This plan does NOT depend on Plan 02-02
     (analysis/preprocess.py) -- it runs independently in the same wave. To get a
     PCA'd input for cluster() tests, call sc.pp.normalize_total/log1p/pca directly
     inside the test setup (a few lines), do NOT import analysis.preprocess. -->

```python
# tests/conftest.py (already exists)
def structured_adata() -> AnnData:
    """~200 cells x ~80 genes, raw integer counts, two well-separated
    pseudo-populations, .obs['true_population'] in {"A", "B"}."""
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cluster() -- neighbors, Leiden (igraph), UMAP with size guards</name>
  <files>analysis/cluster.py, tests/test_cluster.py</files>
  <behavior>
    - Given `structured_adata` with PCA already computed (test setup: `sc.pp.normalize_total` -> `sc.pp.log1p` -> `sc.pp.pca(n_comps=10)` directly, not via `analysis.preprocess`), `cluster(adata, resolution=1.0, n_neighbors=15, random_state=0)` produces `.obs["leiden"]` with `>= 2` unique values, and `.obsm["X_umap"]` with shape `(n_cells, 2)`.
    - Calling `sc.tl.leiden` is never passed `directed=True` -- assert by inspecting the call succeeds without `ValueError` (a regression here would raise `ValueError: Cannot use igraph's leiden implementation with a directed graph.`).
    - Running `cluster()` twice with the same `random_state` on the same PCA'd input produces identical `.obs["leiden"]` label assignments (list equality) -- Leiden determinism is asserted; `.obsm["X_umap"]` coordinate equality is deliberately NOT asserted (Pitfall 5).
    - On a small PCA'd input (Phase 1's `synthetic_adata`, 50 cells, PCA computed with `n_comps` clamped to a valid bound), `cluster()` with default-sized `n_neighbors=15` does not raise -- internal clamping (`min(n_neighbors, n_obs - 1)`) keeps the neighbor search within bounds.
  </behavior>
  <action>
    Implement `analysis/cluster.py::cluster(adata, *, resolution=1.0, n_neighbors=15, n_pcs=None, random_state=0)` per `02-RESEARCH.md` Pattern 2: `adata = adata.copy()`, clamp `n_neighbors = min(n_neighbors, adata.n_obs - 1)`, `sc.pp.neighbors(adata, n_neighbors=n_neighbors, n_pcs=n_pcs, random_state=random_state)`, `sc.tl.leiden(adata, resolution=resolution, flavor="igraph", n_iterations=2, directed=False, random_state=random_state)` (`directed=False` explicit, never omitted -- self-documenting per Pitfall 2), `sc.tl.umap(adata, random_state=random_state)`. Return the mutated `adata` only (summary construction is Task 2). Write `tests/test_cluster.py` first (RED) per `<behavior>`, then implement until green.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cluster.py -x -q</automated>
  </verify>
  <done>`uv run pytest tests/test_cluster.py -x -q` passes -- covers >=2 clusters found, UMAP shape, Leiden-label determinism, and the small-fixture size guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ClusterSummary construction (ANLYS-04 for this module)</name>
  <files>analysis/cluster.py, tests/test_cluster.py</files>
  <behavior>
    - `cluster()` now returns `(adata, ClusterSummary)`. `summary.n_clusters == adata.obs["leiden"].nunique()`, `summary.cluster_sizes` is a `dict[str, int]` keyed by cluster label matching `adata.obs["leiden"].value_counts()`, `summary.resolution == 1.0` (the value used), `summary.umap_computed is True`, `summary.dataset_id is None`, `summary.config` contains `resolution`/`n_neighbors`/`random_state` (post-clamping values).
    - `dataclasses.asdict(summary)` contains no field of length `O(n_cells)` -- `cluster_sizes` is `O(n_clusters)`, never a per-cell array.
  </behavior>
  <action>
    Extend `cluster()` to build and return a `ClusterSummary` (imported from `analysis.summary`) alongside the `AnnData`: `cluster_sizes = adata.obs["leiden"].value_counts().to_dict()` (stringify keys), `n_clusters = len(cluster_sizes)`, `umap_computed = "X_umap" in adata.obsm`, `config` set to post-clamping resolved values, `dataset_id=None`. Update the function signature return type to `tuple[AnnData, ClusterSummary]`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cluster.py -x -q</automated>
  </verify>
  <done>`uv run pytest tests/test_cluster.py -x -q` passes, including the bounded-summary assertions.</done>
</task>

</tasks>

<verification>
`uv run pytest tests/test_cluster.py -q` all green. `uv run pytest tests/ -q` (full suite so far) still green.
</verification>

<success_criteria>
- `cluster()` produces `>=2` Leiden clusters on structured data and a `(n_cells, 2)` UMAP embedding.
- `flavor="igraph"` is always called with `directed=False`, never triggering the `ValueError` Pitfall 2 describes.
- Leiden cluster labels are reproducible given a fixed `random_state`; UMAP coordinate exactness is explicitly not relied upon.
- Returns a `ClusterSummary` whose size never scales with `n_cells`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-analysis-tool-layer/02-03-SUMMARY.md`
</output>
