---
phase: 02-analysis-tool-layer
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified:
  - analysis/preprocess.py
  - tests/test_preprocess.py
autonomous: true
requirements: ["ANLYS-01"]

must_haves:
  truths:
    - "Given a raw-counts AnnData, normalization, HVG selection, and PCA run deterministically as a prerequisite pipeline step"
    - "preprocess() never mutates the caller's input AnnData"
    - "Small test-fixture-sized inputs (fewer genes/cells than the tutorial defaults) do not crash the PCA/HVG solvers"
  artifacts:
    - path: "analysis/preprocess.py"
      provides: "preprocess(adata, **config) -> (AnnData, PreprocessSummary)"
  key_links:
    - from: "analysis/preprocess.py"
      to: "analysis/summary.py:PreprocessSummary"
      via: "constructs and returns PreprocessSummary"
      pattern: "PreprocessSummary\\("
---

<objective>
Build `analysis/preprocess.py` (ANLYS-01): `normalize_total` -> `log1p` -> `highly_variable_genes` -> `pca`, in the exact order ANLYS-01's wording and the current official scanpy tutorial both use, guarded against small inputs, deterministic given a fixed `random_state`, and returning a bounded `PreprocessSummary` alongside the mutated `AnnData`.

Purpose: Per `02-RESEARCH.md` Pattern 1 and Pitfall 3, running HVG selection on non-log data silently produces a wrong (not erroring) selection -- the call order matters and must be enforced by the function itself, not left to caller discipline. Per Pitfall 4, `n_top_genes`/`n_pcs` defaults (2000/50) will raise solver errors on small fixtures unless clamped.
Output: `analysis/preprocess.py`, passing tests.
</objective>

<execution_context>
@/Users/darren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darren/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-analysis-tool-layer/02-RESEARCH.md
@.planning/phases/02-analysis-tool-layer/02-VALIDATION.md
</context>

<interfaces>
<!-- From Plan 02-01 (analysis/summary.py) -- use this dataclass directly, no exploration needed. -->

```python
# analysis/summary.py (already exists from Plan 02-01)
@dataclass
class PreprocessSummary:
    dataset_id: str | None
    n_cells: int
    n_genes_total: int
    n_hvg: int
    n_pcs_computed: int
    variance_ratio_top10: list[float]
    config: dict
```

<!-- From Plan 02-01 (tests/conftest.py) -- use this fixture directly. Also usable: Phase 1's smaller
     `synthetic_adata` (20 genes x 50 cells) for the small-input size-guard test. -->

```python
# tests/conftest.py (already exists)
def structured_adata() -> AnnData:
    """~200 cells x ~80 genes, raw integer counts, two well-separated
    pseudo-populations, .obs['true_population'] in {"A", "B"}."""
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: preprocess() -- normalize/HVG/PCA with size guards and copy semantics</name>
  <files>analysis/preprocess.py, tests/test_preprocess.py</files>
  <behavior>
    - `preprocess(structured_adata, n_top_genes=50, n_pcs=10, random_state=0)` returns an `AnnData` with `.var["highly_variable"]` present (`sum() == min(50, n_vars)`), `.obsm["X_pca"]` present with shape `(n_cells, min(10, n_pcs_bound))`, and `.X` no longer equal to the original raw counts (normalized + log1p'd).
    - The caller's original `structured_adata` object is unmodified after the call (assert original `.X` unchanged, e.g. by comparing to a `.copy()` taken before the call) -- `preprocess()` must not mutate its input in place.
    - Calling `preprocess()` twice with the same `random_state` on the same input produces identical `.obsm["X_pca"]` arrays (`np.allclose`) -- proves determinism.
    - Calling `preprocess()` on Phase 1's small `synthetic_adata` fixture (20 genes, 50 cells) with default-sized `n_top_genes`/`n_pcs` does not raise -- internal clamping (`min(requested, n_obs - 1, n_vars - 1)`) keeps the PCA/HVG solvers within bounds.
  </behavior>
  <action>
    Implement `analysis/preprocess.py::preprocess(adata, *, target_sum=None, n_top_genes=2000, n_pcs=50, random_state=0)` per `02-RESEARCH.md` Pattern 1: `adata = adata.copy()` first (tool-boundary isolation), `sc.pp.normalize_total(adata, target_sum=target_sum)`, `sc.pp.log1p(adata)`, clamp `n_top_genes = min(n_top_genes, adata.n_vars)`, `sc.pp.highly_variable_genes(adata, n_top_genes=n_top_genes)` (default `flavor="seurat"`, expects log data -- do not pass a different flavor), clamp `n_comps = min(n_pcs, adata.n_obs - 1, adata.n_vars - 1)`, `sc.pp.pca(adata, n_comps=n_comps, random_state=random_state)`. Return the mutated `adata` only (summary construction is Task 2). Write `tests/test_preprocess.py` first (RED) per `<behavior>`, then implement until green.
  </action>
  <verify>
    <automated>uv run pytest tests/test_preprocess.py -x -q</automated>
  </verify>
  <done>`uv run pytest tests/test_preprocess.py -x -q` passes -- covers determinism, copy semantics, and the small-fixture size guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PreprocessSummary construction (ANLYS-04 for this module)</name>
  <files>analysis/preprocess.py, tests/test_preprocess.py</files>
  <behavior>
    - `preprocess()` now returns `(adata, PreprocessSummary)`. On `structured_adata` with `n_top_genes=50, n_pcs=10`: `summary.n_cells == 200`, `summary.n_genes_total == 80`, `summary.n_hvg == min(50, 80)`, `summary.n_pcs_computed == min(10, 199, 79)`, `len(summary.variance_ratio_top10) <= 10`, `summary.dataset_id is None` (unset by this low-level function), `summary.config` contains `target_sum`/`n_top_genes`/`n_pcs`/`random_state` matching what was actually used (post-clamping values, not the raw requested values).
    - `dataclasses.asdict(summary)` contains no field whose length scales with `n_cells` or `n_genes_total` (structural bound check -- e.g. assert every list-valued field has `len() <= 10`).
  </behavior>
  <action>
    Extend `preprocess()` to build and return a `PreprocessSummary` (imported from `analysis.summary`) alongside the `AnnData`: pull `variance_ratio_top10` from `adata.uns["pca"]["variance_ratio"][:10].tolist()` (scanpy's `sc.pp.pca` writes this to `adata.uns["pca"]`), set `config` to the post-clamping resolved values actually passed to the scanpy calls (not the caller's raw request), leave `dataset_id=None`. Update the function signature return type to `tuple[AnnData, PreprocessSummary]`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_preprocess.py -x -q</automated>
  </verify>
  <done>`uv run pytest tests/test_preprocess.py -x -q` passes, including the bounded-summary assertions.</done>
</task>

</tasks>

<verification>
`uv run pytest tests/test_preprocess.py -q` all green. `uv run pytest tests/ -q` (full suite so far) still green.
</verification>

<success_criteria>
- `preprocess()` runs normalize -> log1p -> HVG -> PCA in that exact order, matching ANLYS-01 and the official scanpy tutorial.
- Never mutates the caller's `AnnData`; deterministic given a fixed `random_state`.
- Safe on both realistic-sized and small test-fixture-sized inputs.
- Returns a `PreprocessSummary` whose size never scales with dataset size.
</success_criteria>

<output>
After completion, create `.planning/phases/02-analysis-tool-layer/02-02-SUMMARY.md`
</output>
