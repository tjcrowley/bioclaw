---
phase: 10-packaging-local-verification
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - .planning/phases/10-packaging-local-verification/10-02-CLEAN-CHECKOUT-TRANSCRIPT.md
autonomous: false
requirements: [PKG-02]

must_haves:
  truths:
    - "A genuinely clean checkout (fresh git worktree, no pre-existing .venv/data/agent-logs) can run only the documented setup+run steps and successfully serve the app at http://localhost:8000/app"
    - "Darren can complete one continuous browser session exercising login, chat Q&A, live tool-call activity, citation resolution, session list/resume, and dataset upload->ingest, and confirms every v1.1 capability works together end to end"
  artifacts:
    - path: ".planning/phases/10-packaging-local-verification/10-02-CLEAN-CHECKOUT-TRANSCRIPT.md"
      provides: "Captured transcript proving the clean-checkout dry run succeeded against webapp/README.md's documented steps, with no undocumented extra steps"
  key_links:
    - from: "fresh git worktree (no warm .venv/data/agent/logs)"
      to: "uv sync --extra web && uv run --extra web uvicorn webapp.backend.main:app --port 8000"
      via: "the exact command block documented in webapp/README.md (Plan 10-01), executed verbatim with no undocumented steps"
      pattern: "uv sync --extra web"
    - from: "browser session at http://localhost:8000/app"
      to: "all six v1.1 capabilities (login, chat Q&A, live activity, citations, sessions, upload)"
      via: "Darren's single continuous manual walkthrough, approved"
      pattern: "approved"
---

<objective>
Prove PKG-02's two remaining unverified claims for real: (1) that the documented command from Plan 10-01's `webapp/README.md` actually boots the app from a genuinely clean checkout — not just the already-warm dev directory every prior checkpoint reused — and (2) that all six v1.1 capabilities (login, chat Q&A, live tool-call streaming, citation resolution, session list/resume, dataset upload->ingest) work together in one continuous browser session, which no single prior phase checkpoint tested combined.

Purpose: This is the phase-gate plan for the entire v1.1 milestone. Everything up to this point has been verified in isolation (Phase 7: ask/stream, Phase 8: sessions/upload, Phase 9: full UI) or on a warm dev machine. This plan closes the last two literal gaps in PKG-02's wording ("clean checkout" and "confirms every v1.1 capability works together").
Output: A captured clean-checkout dry-run transcript, and Darren's explicit approval of the combined end-to-end browser walkthrough — the final gate before the v1.1 milestone is complete.
</objective>

<execution_context>
@/Users/darren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darren/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-packaging-local-verification/10-RESEARCH.md
@.planning/phases/10-packaging-local-verification/10-VALIDATION.md
@.planning/phases/10-packaging-local-verification/10-01-SUMMARY.md

# The documented command + demo dataset script this plan verifies
@webapp/README.md
@scripts/make_sample_dataset.py
</context>

<interfaces>
<!-- This plan consumes Plan 10-01's outputs directly; no new interfaces created. -->

From webapp/README.md (Plan 10-01) -- the exact command this plan must prove works from a
clean checkout:
```bash
uv sync --extra web
BIOCLAW_WEB_PASSWORD=<your-chosen-password> \
ANTHROPIC_API_KEY=<your-anthropic-api-key> \
uv run --extra web uvicorn webapp.backend.main:app --port 8000
# open http://localhost:8000/app
```

From scripts/make_sample_dataset.py (Plan 10-01) -- generates the demo dataset used for the
upload-flow step of the combined manual checkpoint:
```bash
python scripts/make_sample_dataset.py   # writes data/demo_10x/
```

From tests/test_webapp_integration.py -- the proven, scope-safe question phrasing for the
manual checkpoint's chat Q&A step (deliberately avoids triggering bio-FM annotation tools,
per 10-RESEARCH.md Pitfall 5):
```python
question = "Ingest the dataset at {path} and tell me how many clusters it has."
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Clean-checkout dry run</name>
  <files>.planning/phases/10-packaging-local-verification/10-02-CLEAN-CHECKOUT-TRANSCRIPT.md</files>
  <action>
    Prove PKG-02's "from a clean checkout" claim literally, per 10-RESEARCH.md's Pitfall 4
    and Code Examples. The current dev directory already has a warm `.venv/`, `data/`,
    `agent/logs/`, and `agent/memory.sqlite` from prior phases -- re-running the command
    there proves nothing about a genuinely fresh clone.

    Steps (run and capture real output for each):
    1. `git worktree add /tmp/bioclaw-clean-check HEAD`
    2. `cd /tmp/bioclaw-clean-check` and confirm no `.venv/`, `data/`, or `agent/logs/`
       exist yet (`ls -la` before setup).
    3. Run only the documented setup step: `uv sync --extra web`. Capture full output.
    4. Start the server in the background with a test password:
       `BIOCLAW_WEB_PASSWORD=cleancheck-test ANTHROPIC_API_KEY=$ANTHROPIC_API_KEY uv run --extra web uvicorn webapp.backend.main:app --port 8001 &`
       (use port 8001 to avoid colliding with any already-running dev instance on 8000).
    5. Verify the server actually boots and serves correctly with curl:
       - `curl -s -o /dev/null -w '%{http_code}' http://localhost:8001/app` -> expect `200`
       - `curl -s -X POST http://localhost:8001/api/login -H 'Content-Type: application/json' -d '{"password":"wrong"}' -w '%{http_code}'` -> expect `401`
       - `curl -s -X POST http://localhost:8001/api/login -H 'Content-Type: application/json' -d '{"password":"cleancheck-test"}' -w '%{http_code}'` -> expect `200`
    6. Stop the background server (`kill %1` or the captured PID).
    7. Clean up: `cd` back to the real repo, `git worktree remove /tmp/bioclaw-clean-check --force`.

    Write the full transcript (every command run + its actual output/status codes) to
    `.planning/phases/10-packaging-local-verification/10-02-CLEAN-CHECKOUT-TRANSCRIPT.md`,
    including a one-line PASS/FAIL verdict at the top. If any step deviates from what
    `webapp/README.md` documents (e.g. an extra undocumented step was needed to make it
    work), that is a real PKG-02 gap -- fix `webapp/README.md` (from Plan 10-01) to match
    reality rather than silently working around it, and note the fix in the transcript.

    This task does not touch, deploy to, or provision any remote host -- the worktree is a
    local `/tmp` directory on this machine only.
  </action>
  <verify>
    <automated>test -f .planning/phases/10-packaging-local-verification/10-02-CLEAN-CHECKOUT-TRANSCRIPT.md && grep -q "PASS" .planning/phases/10-packaging-local-verification/10-02-CLEAN-CHECKOUT-TRANSCRIPT.md</automated>
  </verify>
  <done>Transcript file exists, documents every command and its actual output from a genuinely clean git worktree (no pre-existing .venv/data/agent-logs), and records a PASS verdict for: uv sync succeeding, server boot, /app returning 200, wrong password returning 401, correct password returning 200.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify checkpoint — combined end-to-end browser walkthrough of all v1.1 capabilities</name>
  <files></files>
  <what-built>
    Everything the v1.1 milestone has built across Phases 7-10: a password-gated FastAPI
    backend (login, ask, WebSocket tool-activity streaming, session list/resume, dataset
    upload->ingest) and an OpenClaw-styled vanilla-JS frontend consuming it — now with a
    documented single run command (Plan 10-01's webapp/README.md), a permanent
    self-containment regression test, and a proven clean-checkout boot (Task 1 above).
    This checkpoint is the first time all six capabilities are exercised together in one
    continuous session, rather than individually per prior phase.
  </what-built>
  <how-to-verify>
    In your normal dev checkout (not the temporary /tmp worktree -- that one already has
    ANTHROPIC_API_KEY and real session history available):

    1. Stop any already-running instance, then start fresh:
       `BIOCLAW_WEB_PASSWORD=<your-password> ANTHROPIC_API_KEY=<your-key> uv run --extra web uvicorn webapp.backend.main:app --port 8000`
    2. Open `http://localhost:8000/app` in a browser.
    3. Login (UI-06): confirm you cannot see any chat UI before entering the correct
       password; confirm an incorrect password is rejected with a visible error; confirm
       the correct password logs you in.
    4. Chat Q&A + live activity (UI-01, UI-02): run `python scripts/make_sample_dataset.py`
       first if you haven't already (writes `data/demo_10x/`), then ask:
       `Ingest the dataset at data/demo_10x and tell me how many clusters it has.`
       Watch the live tool-call activity view update in real time as ingest/analyze tools
       run (not just a final answer appearing with no visible activity beforehand).
    5. Citations (UI-03): click a citation tag (`[ref:...]`) in the answer and confirm
       it resolves to the underlying audit log entry rather than displaying as raw bracket
       text.
    6. Session sidebar (UI-04): confirm the just-completed session appears in the
       sidebar; click it (or start a new question, then navigate back to it) and confirm
       the prior thread/context is restored correctly.
    7. Dataset upload (UI-05): using the composer's upload control (drag-and-drop or
       file picker), upload the `data/demo_10x/` files directly (not via chat text) and
       confirm ingest progress/result appears inline in the thread.
    8. Confirm all of the above happened in one continuous session without needing to
       restart the server or re-authenticate mid-walkthrough.
  </how-to-verify>
  <action>Run the combined browser walkthrough exactly as described in how-to-verify, in order, in a single continuous session. This is the human-verify step -- Claude pauses here for Darren's confirmation before the v1.1 milestone can be closed.</action>
  <verify>
    <automated>echo "Manual verification required -- see how-to-verify above"</automated>
  </verify>
  <done>All six v1.1 capabilities (login, chat Q&A, live tool-call activity, citation resolution, session list/resume, dataset upload-&gt;ingest) are confirmed working together in one continuous browser session, and Darren has typed "approved" (or equivalent sign-off).</done>
  <resume-signal>Type "approved" once all six capabilities are confirmed working together in one session, or describe exactly what broke.</resume-signal>
</task>

</tasks>

<verification>
Phase-gate verification for the entire v1.1 milestone:
1. Task 1's clean-checkout transcript shows a PASS verdict with no undocumented steps.
2. Task 2's combined manual walkthrough is approved by Darren.
3. Re-confirm the fast-tier suite is still green immediately before closing the phase:
   `uv run --extra web pytest tests/ -x -q -m "not live_llm and not bio_fm_smoke and not vcc_data"`
</verification>

<success_criteria>
- A genuinely clean checkout (fresh git worktree) boots the documented command successfully with no undocumented steps, evidenced by a captured transcript.
- Darren has approved one continuous browser session exercising all six v1.1 capabilities together end to end.
- No step in this plan deployed, or required deploying, to DigitalOcean or any other remote/production environment — everything ran on localhost or a local /tmp worktree.
- v1.1 milestone (Phases 7-10, PKG-01/PKG-02 + all prior UI-*/API-* requirements) is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/10-packaging-local-verification/10-02-SUMMARY.md`
</output>
