---
phase: 05-perturbation-response-tool-vcc-benchmark-harness
plan: 06
type: execute
wave: 5
depends_on: ["05-05"]
files_modified:
  - pyproject.toml
  - benchmark/download_vcc.py
  - tests/test_vcc_smoke.py
autonomous: false
requirements: ["VCC-01", "VCC-02", "VCC-03"]
user_setup:
  - service: VCC public dataset (Google Cloud Storage, Requester Pays)
    why: "The vcc_data phase-gate smoke test needs the real Virtual Cell Challenge public train/test split downloaded and cached locally -- streaming/downloading from gs://arc-institute-virtual-cell-atlas/virtual-cell-challenge/ requires an authenticated gcloud/GCS identity, and the bucket is subject to Requester Pays billing beyond a 2TB/month free tier (05-RESEARCH.md Pitfall 7)"
    env_vars:
      - name: GOOGLE_APPLICATION_CREDENTIALS or an active `gcloud auth login` session
        source: "Google Cloud Console -> IAM & Admin -> Service Accounts (or run `gcloud auth application-default login` interactively)"
    dashboard_config:
      - task: "If Task 1's automated download attempt fails on authentication or a Requester Pays billing-project prompt, run `gcloud auth login` and `gcloud config set project <your-billing-project>` manually, then re-run the download command documented in benchmark/download_vcc.py's module docstring"
        location: "gcloud CLI (already available if gcloud is installed) or Google Cloud Console"

must_haves:
  truths:
    - "The real VCC public dataset (not a synthetic fixture) has been ingested through ingest/loaders.py::load() -> ingest_10x, proving VCC-01 against real data, not just the .h5ad-branch unit test"
    - "run_full_benchmark() has been run at least once against real VCC data, producing a real MAE/PDS/DES report for both the linear-additive predictor and the naive baseline"
    - "The vcc_data-marked smoke test is excluded from the default fast-tier test run, mirroring bio_fm_smoke's established precedent"
  artifacts:
    - path: "benchmark/download_vcc.py"
      provides: "a scripted attempt to download the VCC public train/validation/test split from gs://arc-institute-virtual-cell-atlas/virtual-cell-challenge/ into a local cache directory"
      exports: ["download_vcc_split"]
    - path: "tests/test_vcc_smoke.py"
      provides: "vcc_data-marked end-to-end test: real .h5ad ingest -> DatasetStore -> run_full_benchmark() against the real downloaded VCC split"
  key_links:
    - from: "tests/test_vcc_smoke.py"
      to: "benchmark/report.py:run_full_benchmark"
      via: "real (unmocked) end-to-end call, marked @pytest.mark.vcc_data"
      pattern: "vcc_data"
---

<objective>
Close the phase's last gap: attempt an automated download of the real VCC public dataset, write the `vcc_data`-marked phase-gate smoke test that ingests it through the existing pipeline and runs the full benchmark end to end, and hand off to a blocking human-verify checkpoint for whatever the download automation cannot complete -- mirroring Phase 4's `04-05` plan exactly.

Purpose: Every prior Phase 5 plan (05-01 through 05-05) proved its piece against small synthetic fixtures only, by design (05-VALIDATION.md's Manual-Only Verifications row explicitly defers the real, authenticated, multi-GB VCC download to this final phase gate rather than firing it on every task/CI run). This plan is where PERT-01/02 and VCC-01/02/03 all get their one real-data confirmation: 05-RESEARCH.md Pitfall 4 (verify actual `.obs` column/control-token values against the real data, not assumed defaults) and Pitfall 6 (respect VCC's own published train/validation/test partition, not a re-split) can only be checked here, against real data, not a controlled fixture.
Output: `benchmark/download_vcc.py` with a scripted download attempt, a `vcc_data`-marked `tests/test_vcc_smoke.py` ready to run once the data exists, and a demonstrated real end-to-end benchmark report.
</objective>

<execution_context>
@/Users/darren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darren/.claude/get-shit-done/templates/summary.md
@/Users/darren/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-perturbation-response-tool-vcc-benchmark-harness/05-RESEARCH.md
@.planning/phases/05-perturbation-response-tool-vcc-benchmark-harness/05-VALIDATION.md
@.planning/phases/04-bio-fm-cell-type-annotation/04-05-PLAN.md
@ingest/loaders.py
@benchmark/report.py
@perturbation/pipeline.py
</context>

<interfaces>
From `benchmark/report.py` (already implemented by Plan 05-05):
```python
def run_full_benchmark(name, target_genes, version=None, store_root="data") -> dict:
    """Full report dict: dataset_id, target_genes, predictor{mae,pds,des}, baseline{mae,pds,des}, qc_provenance_note."""
```

From `ingest/loaders.py` (already implemented by Plan 05-01):
```python
def load(path: str | Path) -> AnnData:
    """Dispatches directories -> sc.read_10x_mtx, .h5 -> sc.read_10x_h5, .h5ad -> sc.read_h5ad."""
```

05-RESEARCH.md Pitfall 4/7 (must be checked against the real download, not assumed):
- Verify the real VCC `.h5ad`'s actual `.obs` column name and control-perturbation token string (`"non-targeting"` vs `"ntc"` vs dataset-specific) before calling anything with hardcoded defaults.
- Bucket path is `gs://arc-institute-virtual-cell-atlas/virtual-cell-challenge/` (old `arc-ctc-tahoe100`/`arc-scbasecount` paths are deprecated/404).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Scripted VCC dataset download attempt</name>
  <files>pyproject.toml, benchmark/download_vcc.py</files>
  <action>
    Check whether `gcloud`/`gsutil` is available (`which gcloud gsutil`). If a GCS-aware Python approach is preferred instead, run `uv add gcsfs` (05-RESEARCH.md's Supporting stack entry) and use it for a streaming/cached download rather than shelling out -- pick whichever is more reliable given what's actually installed in this environment; document the choice.

    Fetch the current README/documentation at `github.com/ArcInstitute/arc-virtual-cell-atlas` (WebFetch or curl) to confirm the exact current file paths under `gs://arc-institute-virtual-cell-atlas/virtual-cell-challenge/` (train/validation/test split names may not match this research's assumptions exactly -- verify, don't guess).

    Implement `benchmark/download_vcc.py::download_vcc_split(split, out_dir="data/vcc_raw", billing_project=None) -> Path` (`split` is one of `"train"`, `"validation"`, `"test"`, or whatever the real bucket's actual directory names turn out to be): attempts the download/copy (via `gcloud storage cp -r` or `gcsfs`, whichever was chosen above) into `out_dir`, returning the local path. If the attempt fails (auth error, Requester Pays billing-project prompt, 403/404), do not attempt to script around it -- catch the failure, log/print the exact error, and let it propagate or return `None` with a clear printed message. Document in the module docstring: this is a one-time-per-machine command, not something re-run automatically; also document the exact `gcloud auth login` / billing-project-setting steps a human would need if automation fails (this becomes Task 3's checkpoint instructions).

    Actually attempt the download now for at least one small split (start with the smallest available split, not the full training set, to keep this task's own runtime reasonable) and record the outcome (success + local path, or the specific failure) for this plan's SUMMARY.
  </action>
  <verify>
    <automated>uv run python -c "from benchmark.download_vcc import download_vcc_split; print('ok')"</automated>
  </verify>
  <done>`benchmark/download_vcc.py::download_vcc_split()` is implemented and was actually attempted at least once; its outcome (success with a real local path, or a specific documented failure) is recorded for Task 3's checkpoint to act on.</done>
</task>

<task type="auto">
  <name>Task 2: vcc_data-marked end-to-end smoke test</name>
  <files>tests/test_vcc_smoke.py</files>
  <action>
    Write `tests/test_vcc_smoke.py`, module-level `pytestmark = pytest.mark.vcc_data`:
    ```python
    import pytest
    pytestmark = pytest.mark.vcc_data

    def test_real_vcc_ingest_and_benchmark(tmp_path):
        # 1. Load the real downloaded VCC .h5ad (path from benchmark/download_vcc.py's
        #    documented local cache location) via ingest.loaders.load() -> ingest.pipeline.ingest_10x,
        #    proving VCC-01 against real data.
        # 2. Inspect adata.obs columns/control-token values directly; assert whatever the
        #    real values turn out to be (document them here once known -- do not assume
        #    "target_gene"/"non-targeting" without checking, per Pitfall 4).
        # 3. Persist into a DatasetStore at tmp_path (or a fixed local data/ root -- your call).
        # 4. Call benchmark.report.run_full_benchmark(name, target_genes=[...a handful of real
        #    target genes from the actual data...], store_root=...).
        # 5. Assert the report has both predictor and baseline sections with real MAE/PDS/DES
        #    values (not asserting specific score thresholds -- just that real numbers came back).
        ...
    ```
    Build this test so it is self-contained and runnable via `uv run pytest tests/test_vcc_smoke.py -m vcc_data -x -v -s` once the real data exists locally (per Task 1's download location), without further setup.
  </action>
  <verify>
    <automated>uv run pytest tests/ -q -m "not live_llm and not bio_fm_smoke and not vcc_data" && uv run python -m py_compile tests/test_vcc_smoke.py</automated>
  </verify>
  <done>`tests/test_vcc_smoke.py` exists, is marked `vcc_data`, and is excluded from the default fast-tier run (verified by the first half of the verify command staying green); it is ready to execute once real VCC data is available locally.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify real VCC dataset download + end-to-end benchmark smoke test</name>
  <files>data/vcc_raw/</files>
  <action>
    Everything automatable has already been done by Tasks 1-2: the download was attempted, and the vcc_data smoke test is written and ready. This checkpoint exists only for whatever automation could not complete (typically an interactive `gcloud auth login` step or a Requester Pays billing-project confirmation that no unattended script can approve) and for a human sanity check on the real benchmark numbers.
  </action>
  <what-built>
    A scripted VCC dataset download attempt (`benchmark/download_vcc.py`), and a `vcc_data`-marked end-to-end test (`tests/test_vcc_smoke.py`) that ingests the real data through the existing pipeline and runs the full PDS/DES/MAE benchmark with naive-baseline comparison.
  </what-built>
  <how-to-verify>
    1. If Task 1's automated download failed, run `gcloud auth login` (and set a billing project if prompted for Requester Pays) then re-run the download command documented in `benchmark/download_vcc.py`'s module docstring.
    2. Run `uv run pytest tests/test_vcc_smoke.py -m vcc_data -x -v -s` and report: did it pass, and what were the real MAE/PDS/DES numbers for the linear-additive predictor vs. the naive baseline?
    3. Confirm the predictor's scores are at least directionally sane relative to the baseline (per 05-RESEARCH.md Pitfall 6, suspiciously perfect scores matching/exceeding published leaderboard numbers on the first attempt would indicate train/test leakage, not real skill -- flag if you see that).
    4. If the smoke test fails with an auth/billing error rather than a data-shape error, the download itself likely never completed -- check `benchmark/download_vcc.py`'s documented failure output first before debugging the ingest/benchmark path itself.
  </how-to-verify>
  <verify>
    <automated>MISSING -- inherently manual: requires an authenticated, potentially-billed real GCS download and real (non-mocked) benchmark computation, excluded from CI via the vcc_data marker (see Task 2)</automated>
  </verify>
  <done>Human reports "passed" with real predictor vs. baseline MAE/PDS/DES numbers, or a documented reason it could not be completed (billing/auth blocker, etc.).</done>
  <resume-signal>Report "passed, predictor: {mae, pds, des}, baseline: {mae, pds, des}" or describe what failed (auth/billing issue, data-shape mismatch, suspicious leakage-like scores, etc.)</resume-signal>
</task>

</tasks>

<verification>
`uv run pytest tests/ -q -m "not live_llm and not bio_fm_smoke and not vcc_data"` fully green (unchanged from Wave 4 -- this plan adds no new fast-tier tests). The `vcc_data` smoke test has been run at least once manually with a recorded real-data outcome, per the checkpoint above.
</verification>

<success_criteria>
- VCC-01/02/03 are all confirmed against real VCC data, not just synthetic fixtures -- closing the phase's own explicit "independently validated against Arc Institute's public benchmark" goal.
- The real benchmark run's provenance (which QC state, which split, real column/token names found) is documented, per 05-RESEARCH.md Pitfall 5.
- Phase 5's success criteria are all demonstrably true: perturbation prediction from control + target gene, automatic naive-baseline comparison, VCC ingest through the existing pipeline, and a real PDS/DES/MAE report with baseline comparison.
</success_criteria>

<output>
After completion, create `.planning/phases/05-perturbation-response-tool-vcc-benchmark-harness/05-06-SUMMARY.md`
</output>
