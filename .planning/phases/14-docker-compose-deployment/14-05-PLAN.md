---
phase: 14-docker-compose-deployment
plan: 05
type: execute
wave: 4
depends_on: ["14-03", "14-04"]
files_modified:
  - docker/README.md
  - README.md
autonomous: false
requirements: [DOCK-01]

must_haves:
  truths:
    - "docker compose up from a clean checkout, with only env vars configured, starts a working backend, frontend, and optional GPU worker -- no manual setup steps required beyond env vars"
    - "A researcher can perform the complete workflow (upload or census-fetch, analysis, FM inference, CSV export, scanpy script export) against the compose stack with no locally-installed Python dependencies"
    - "If any automated FM-artifact fetch failed during the Wave 3 build, the exact manual fallback is documented so the stack is still reachable"
  artifacts:
    - path: "docker/README.md"
      provides: "documents the Dockerfile's automated-fetch design and exact manual fallback for scGPT checkpoint / Geneformer clone if automation failed"
    - path: "README.md"
      provides: "top-level Docker usage section (CPU-only and GPU-enabled invocations)"
  key_links:
    - from: "README.md Docker section"
      to: "docker-compose.yml / docker-compose.gpu.yml (Plan 14-04)"
      via: "documented docker compose up / docker compose -f ... -f ... up commands"
      pattern: "docker compose"
    - from: "docker/README.md manual fallback"
      to: "bio_fm_worker/README.md and geneformer_worker/README.md's proven fetch commands"
      via: "cross-reference to the exact commands used in Plan 14-03's builder stages"
      pattern: "gdown|git lfs pull"
---

<objective>
Close out Phase 14: run the full Compose stack for real (not just per-stage builds), document the deployment for future use (including the manual fallback path if any Wave 3 automated fetch failed), and get human confirmation that the complete researcher workflow — upload/census-fetch, analysis, FM inference, CSV export, scanpy script export — works against the running stack with zero locally-installed Python dependencies.

Purpose: this is where DOCK-01's three success criteria get their real, non-hypothetical verification — Plans 14-01 through 14-04 built and unit-verified the pieces; this plan proves they work together as the actual product experience.
Output: a confirmed-working `docker compose up` deployment, `docker/README.md`, an updated top-level `README.md`, and human sign-off on the full workflow.
</objective>

<execution_context>
@/Users/darren/.claude/get-shit-done/workflows/execute-plan.md
@/Users/darren/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-docker-compose-deployment/14-VALIDATION.md
@.planning/phases/14-docker-compose-deployment/14-RESEARCH.md
@bio_fm_worker/README.md
@geneformer_worker/README.md
@webapp/README.md
@docker-compose.yml
@docker-compose.gpu.yml
@scripts/docker_compose_smoke_test.sh
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the real smoke test against the full image</name>
  <files>docker/README.md</files>
  <action>
    Run `scripts/docker_compose_smoke_test.sh` for real against the complete
    stack (all three venvs baked in from Plan 14-03, Compose wiring from
    Plan 14-04). Confirm:
    - `docker compose build` succeeds end to end from the current repo state
      (this is the first time the FULL multi-stage Dockerfile — main +
      scGPT + reference + Geneformer builders — has actually been built in
      one pass; Plans 14-02/14-03 only verified individual `--target` stages
      and the smoke test in 14-04 was syntax-only).
    - `docker compose config` still shows no `command:`/`entrypoint:`
      override (DOCK-01 criterion 2, re-confirmed against the final compose
      files, not just the base).
    - `docker compose up -d` starts a healthy container reachable at
      `http://localhost:8000/api/health`.
    - `docker compose down` cleans up.

    If the Wave 3 build (Plan 14-03) had any failed automated fetch (gdown
    checkpoint, Geneformer LFS pull, or census reference build), document the
    exact failure here — this becomes the "Known gaps" section of
    `docker/README.md` created in Task 2, and is what Task 3's checkpoint
    will need to manually verify/work around instead of assuming it's
    already fixed.
  </action>
  <verify>
    <automated>scripts/docker_compose_smoke_test.sh</automated>
  </verify>
  <done>The smoke test script exits 0 against the real, fully-built image, or — if it does not — the exact failure point and cause is captured for Task 2's documentation and Task 3's checkpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Write docker/README.md and update top-level README.md</name>
  <files>docker/README.md, README.md</files>
  <action>
    Create `docker/README.md` documenting:
    - What the Dockerfile automates (main venv via `uv sync`, scGPT venv +
      checkpoint via `gdown`, Geneformer venv + clone + LFS pull + CUDA
      patch, cellxgene-census reference index) and why each stage is
      isolated (three incompatible Python/dependency requirements per
      `bio_fm_worker/README.md` and `geneformer_worker/README.md`).
    - The exact manual fallback if any automated fetch step fails at build
      time (Google Drive rate-limit/interactive-confirmation wall for the
      scGPT checkpoint; Hugging Face LFS auth/network issues for
      Geneformer), pointing to the same proven commands already documented
      in `bio_fm_worker/README.md`/`geneformer_worker/README.md` — e.g.
      manually populating `bio_fm_worker/checkpoints/scGPT_human/` on the
      host and adding a bind mount, or building with `--build-arg`/staged
      manual `docker cp` into a running container as a stopgap.
    - Any "Known gaps" discovered during Task 1's real build run.

    Update top-level `README.md` with a new "Docker" section (near existing
    setup/usage instructions) documenting:
    ```
    ## Docker

    CPU-only (always works, no GPU required):
        docker compose up

    GPU-enabled (adds NVIDIA device passthrough to the same backend service):
        docker compose -f docker-compose.yml -f docker-compose.gpu.yml up

    Configure `.env` from `.env.example` first (ANTHROPIC_API_KEY, BIOCLAW_WEB_PASSWORD).
    Backend serves both the API and the built frontend at http://localhost:8000.
    ```
  </action>
  <verify>
    <automated>test -f docker/README.md && grep -q "## Docker" README.md</automated>
  </verify>
  <done>`docker/README.md` exists documenting automation + manual fallback + any known gaps; top-level `README.md` has a Docker usage section covering both CPU-only and GPU-enabled invocations.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    A complete `docker compose up` deployment (backend + frontend served
    together, optional GPU override) with all three FM-worker Python
    environments baked into the image, persistent state via a named volume,
    and a hard-coded single-worker constraint that cannot be bypassed via
    Compose.
  </what-built>
  <how-to-verify>
    1. Ensure `.env` is configured (copy `.env.example`, fill in
       `ANTHROPIC_API_KEY` and `BIOCLAW_WEB_PASSWORD`).
    2. Run `docker compose up` (add `-f docker-compose.yml -f
       docker-compose.gpu.yml` instead if testing GPU passthrough on a
       CUDA-capable host).
    3. Open `http://localhost:8000` in a browser.
    4. Perform the full researcher workflow with NO locally-installed Python
       dependencies on the host:
       - Upload a dataset OR trigger a cellxgene-census fetch.
       - Run an analysis (clustering/annotation pipeline).
       - Call FM inference (scGPT and/or Geneformer, whichever the Wave 3
         build succeeded in baking in — see `docker/README.md`'s "Known
         gaps" if one was skipped).
       - Download the CSV export.
       - Export the scanpy script.
    5. Run `docker compose down && docker compose up` again and confirm
       previously created session/state data is still present (proves the
       `bioclaw-state` volume persistence).
  </how-to-verify>
  <resume-signal>Type "approved" if the full workflow succeeded end to end, or describe which step failed.</resume-signal>
</task>

</tasks>

<verification>
Real `docker compose build && up` succeeds against the complete multi-venv image; the full researcher workflow (upload/fetch, analysis, FM inference, CSV export, script export) is confirmed by human verification with no local Python installed; state persists across `down`/`up`.
</verification>

<success_criteria>
- DOCK-01 criterion 1 confirmed: clean-checkout `docker compose up` + env vars only, no manual setup.
- DOCK-01 criterion 2 confirmed: `--workers 1` is structural, re-verified against the final compose config.
- DOCK-01 criterion 3 confirmed via human checkpoint: full researcher workflow works with zero local Python dependencies.
- Any gaps from automated FM-artifact fetching are documented with a working manual fallback, not silently left broken.
</success_criteria>

<output>
After completion, create `.planning/phases/14-docker-compose-deployment/14-05-SUMMARY.md`
</output>
