"""Composed cell-type annotation pipeline (ANNOT-01/03).

Stub -- Plan 04-04 implements `annotate()` here, composing baseline.py's
and fm_client.py's calls into one AnnotationSummary. Mirrors
analysis/pipeline.py::analyze().
"""
